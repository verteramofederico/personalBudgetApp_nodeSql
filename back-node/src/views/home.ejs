<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Balance</title>
</head>
<body>
    <h1>Home</h1>

    <form method="POST" action="/create">
        <label for="type">Type</label>
            <select name="type" >
                <option value=income>Income</option>
                <option value=expense>Expense</option>
            </select>

        <label for="concept">Concept</label>
            <input type="text" name="concept" placeholder="Ej: lunch">

        <label for="amount">Amount</label>
            <input type="text" name="amount" placeholder="Ej: 1500">

        <label for="date">Date</label>
            <input type="date" name="date" min="2020-01-01" max="2022-12-31">

        <button type="submit" >Submit</button>
    </form>

    <h2>Incomes</h2>

    <% let sumIncomes = 0 %> 
    <% for( let index = 0; index < incomes.length; index++ ) { %>
        <% sumIncomes += incomes[index].amount %> 
        <%= incomes[index].active === 1 ? incomes[index].concept : "registro eliminado" %>
        <%= incomes[index].active === 1 ? incomes[index].amount : "registro eliminado" %>
        <%= incomes[index].active === 1 ? incomes[index].date : "registro eliminado" %>
        <br>    
    <% } %>
    <h4>Total: <%= sumIncomes %> </h4>

    <h2>Expenses</h2>

    <% let sumExpenses = 0 %> 
    <% for( let index = 0; index < expenses.length; index++ ) { %>
        <% sumExpenses += expenses[index].amount %> 
        <%= expenses[index].active === 1 ? expenses[index].concept : "registro eliminado" %>
        <%= expenses[index].active === 1 ? expenses[index].date : "registro eliminado" %>
        <%= expenses[index].active === 1 ? expenses[index].amount : "registro eliminado" %>
        <br>    
    <% } %>
        <h4>Total: <%= sumExpenses %> </h4>

</body>
</html>